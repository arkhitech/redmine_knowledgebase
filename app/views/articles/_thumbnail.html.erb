

<% article.attachments.reverse_each do |attach| %>
  <% if attach.thumbnailable? %>

    <%= link_to image_tag( thumbnail_path(attach)),
      knowledgebase_article_path(article.id, project_id: @project), :title => article.title %>
    <% break %>
  <% end %>
<% end %>
